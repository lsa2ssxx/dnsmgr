# 彩虹聚合 DNS 管理系统 — 宝塔搭建教程

本文介绍在宝塔面板上部署 **dnsmgr（彩虹聚合 DNS 管理系统）** 的完整步骤。

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| PHP | **8.2 及以上**（推荐 8.2 或 8.3） |
| MySQL | 5.6+（推荐 5.7 或 8.0） |
| Web 服务器 | Nginx 或 Apache |

**PHP 扩展（宝塔需安装）：**  
`curl`、`ftp`、`gd`、`mbstring`、`openssl`、`pdo`、`pdo_mysql`、`sockets`、`ssh2`

---

## 二、宝塔环境准备

### 2.1 安装宝塔面板（未安装时）

```bash
# Centos
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh

# Ubuntu/Deepin
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh

# Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && bash install.sh
```

安装完成后按提示保存面板地址、用户名和密码。

### 2.2 安装 PHP、MySQL、Nginx

1. 登录宝塔面板。
2. **软件商店** → 安装：
   - **Nginx**（或 Apache）
   - **MySQL 5.7** 或 **MySQL 8.0**
   - **PHP 8.2** 或 **PHP 8.3**

### 2.3 安装并启用 PHP 扩展

1. **软件商店** → 找到已安装的 **PHP 8.2**（或 8.3）→ **设置**。
2. 在 **安装扩展** 中安装：
   - `curl`、`ftp`、`gd`、`mbstring`、`openssl`、`opcache`（可选）
   - `pdo_mysql`
   - **ssh2**（若列表中有；若没有，可在「编译安装」或「安装扩展」里找，或通过 pecl 安装）

若宝塔没有 ssh2，可尝试在 SSH 中执行（以 PHP 8.2 为例，路径按你宝塔实际为准）：

```bash
# 示例路径，请根据宝塔实际 PHP 路径修改
/www/server/php/82/bin/pecl install ssh2
```

安装后记得在 php.ini 中启用 `extension=ssh2.so` 并重启 PHP。

---

## 三、创建网站与数据库

### 3.1 添加网站

1. **网站** → **添加站点**。
2. 填写：
   - **域名**：你的域名或临时用 IP（如 `dns.yourdomain.com`）。
   - **根目录**：如 `/www/wwwroot/dnsmgr`（可自定义）。
   - **FTP**、**数据库**：选择 **创建 MySQL 数据库**，记住数据库名、用户名、密码。
   - **PHP 版本**：选择 **PHP-82** 或 **PHP-83**。
3. 提交后等待站点创建完成。

### 3.2 记录数据库信息

在 **数据库** 列表中找到刚创建的数据库，记下：

- 数据库名
- 用户名
- 密码
- 访问权限：一般选 **本地服务器** 即可

---

## 四、部署程序

### 4.1 获取安装包

- **方式一（推荐）：** 从 [Release 页面](https://github.com/netcccyun/dnsmgr/releases) 下载已打包好的 **安装包**（非 Source code），解压后上传到网站根目录（注意是解压后的内容在根目录，见下）。
- **方式二：** 下载 **Source code** 源码包，则需要在服务器上执行 `composer install --no-dev`（见 4.3）。

### 4.2 上传并解压

1. 将下载的压缩包上传到网站根目录（如 `/www/wwwroot/dnsmgr`）。
2. 在宝塔 **文件** 中选中压缩包 → **解压**。
3. 保证目录结构为：
   - 网站根目录下有：`app`、`config`、`public`、`route`、`runtime`、`vendor`（若已带依赖）、`composer.json` 等。
   - 即：**不要把解压出来的「子文件夹」再当根目录**，要把 `app`、`public` 等直接放在站点根目录下。

若解压后多了一层目录（例如多了一个 `dnsmgr-xxx`），把该目录里的所有文件移动到网站根目录即可。

### 4.3 若为 Source code 源码包（需要 Composer）

1. 宝塔 **软件商店** → 安装 **Composer**（若未安装）。
2. SSH 登录服务器，进入网站根目录：

```bash
cd /www/wwwroot/dnsmgr   # 改成你的实际路径
```

3. 安装依赖（生产环境去掉开发依赖）：

```bash
composer install --no-dev
```

如 composer 命令不在 PATH，可用宝塔提供的路径，例如：

```bash
/www/server/php/82/bin/php /usr/bin/composer install --no-dev
```

（路径以你宝塔实际为准。）

---

## 五、宝塔网站设置（必做）

### 5.1 设置网站运行目录为 public

1. **网站** → 找到该站点 → **设置**。
2. **网站目录**：
   - 将 **运行目录** 改为 **`/public`**（相对网站根目录）。
   - 勾选「防跨站」时，一般保持默认即可。
3. 保存。

### 5.2 设置伪静态（ThinkPHP）

1. 同一站点 **设置** → **伪静态**。
2. 在规则框中选择或粘贴：

**Nginx：**

```nginx
location ~* (runtime|application)/ {
    return 403;
}
location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=$1 last; break;
    }
}
```

**Apache：**

```apache
<IfModule mod_rewrite.c>
  Options +FollowSymlinks -Multiviews
  RewriteEngine On

  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php/$1 [QSA,PT,L]
</IfModule>
```

3. 保存。

### 5.3 目录权限（如遇写入失败再调整）

一般宝塔创建的站点目录权限已可写。若安装或运行时提示 `runtime` 等目录不可写，可在 **文件** 中：

- 将 `runtime` 目录权限设为 **755** 或 **775**；
- 所属用户组与网站运行用户一致（如 `www`）。

---

## 六、访问安装向导

1. 浏览器访问你的站点地址（如 `https://dns.yourdomain.com`）。
2. 应自动跳转到 **安装页面**。
3. 按页面提示填写：
   - **数据库地址**：一般为 `127.0.0.1` 或 `localhost`
   - **数据库名**、**用户名**、**密码**：与 3.2 中记录一致
   - **表前缀**：默认即可（如 `dns_`）
   - **端口**：3306
4. 提交后等待安装完成。
5. 安装完成后会跳转到首页，使用你设置的管理员账号登录即可。

**说明（首次安装与数据库）：**  
首次安装时，程序会执行 `app/sql/install.sql`，**自动创建当前版本完整表结构**（已包含多代理 `proxy_id`、容灾 Hook `hook_enable`/`hook_cmd`、日志表 `dnsmgr_log.data` 为 TEXT 等）。安装时会将数据库版本号写入为与代码一致，因此**无需再执行 update.sql**。后续若从本仓库更新代码，只需覆盖文件后用管理员账号访问后台首页，程序会按版本号自动执行 `update.sql` 完成升级。

---

## 七、HTTPS（可选）

1. 站点 **设置** → **SSL**。
2. 选择 **Let's Encrypt** 申请免费证书，或上传自己的证书。
3. 开启 **强制 HTTPS**（建议）。

---

## 八、常见问题

| 现象 | 处理思路 |
|------|----------|
| 500 或空白页 | 看站点 **日志**（Nginx/Apache、PHP 错误日志）；检查运行目录是否为 `public`、伪静态是否已保存。 |
| 提示 PHP 版本不符 | 站点设置里将 PHP 版本改为 8.2 或 8.3，并重启 PHP。 |
| 缺少扩展（如 ssh2） | 在对应 PHP 版本的「安装扩展」中安装，或在 SSH 用 pecl 安装后重启 PHP。 |
| 数据库连接失败 | 核对数据库名、用户、密码、主机（127.0.0.1）、端口；确认数据库为「本地」权限。 |
| 安装页无法访问 | 确认运行目录为 `public`，伪静态为 ThinkPHP 规则，并清除浏览器缓存。 |
| 容灾/定时任务未执行 | 部分功能依赖计划任务；可在宝塔 **计划任务** 中根据官方文档添加对应 cron（如有提供）。 |

---

## 九、后续更新

- 从 [Release](https://github.com/netcccyun/dnsmgr/releases) 下载新版本安装包。
- 上传并解压覆盖除 `runtime`、`.env` 等可能包含本地配置/缓存的目录外的文件（若说明中有特别要求，以官方为准）。
- 覆盖后访问一次前台，如有需要再在后台检查更新说明。

---

**项目地址：** [netcccyun/dnsmgr](https://github.com/netcccyun/dnsmgr)  
**作者：** 消失的彩虹海 — https://blog.cccyun.cn
